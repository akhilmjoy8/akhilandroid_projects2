<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.teacher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeTableActivity" />
        <activity android:name=".ClassEntry" />
        <activity android:name=".Subjects.SubjectsHome" />
        <activity android:name=".Students.StudentsHome" />
        <activity android:name=".Students.StudentDetailsMult" />
        <activity
            android:name=".Home.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Students.StudentAttendance"
            android:label="@string/title_activity_student_attendance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Students.Attendance"
            android:label="@string/title_activity_attendance"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".Home.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>